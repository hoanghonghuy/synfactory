services:
  api:
    volumes:
      - ${SYNFACTORY_TERMINAL_TARGETS_HOST:-./config/terminal-targets.json}:/etc/synfactory/terminal-targets.json:ro
      - ${SYNFACTORY_TERMINAL_SSH_DIR_HOST:-./secrets/terminal-ssh}:/run/secrets/synfactory-terminal:ro
